syntax = "v1"

import "vcode_type.api"

@server(
	prefix: /api/v1/vcode/email
	group: email
)
service Vcode {
	@handler Login
	post /login (EmailReq) returns (EmailResp)

	@handler ChangePassword
	post /change-password (EmailReq) returns (EmailResp)
}